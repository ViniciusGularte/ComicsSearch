import React from "react";
import Head from "next/head";

export default function HeaderWidget({ name = "BattleCoomic" }) {
  <Head>
    <title>{name}</title>
    <link rel="icon" href="/favicon.ico" />
  </Head>;
}
